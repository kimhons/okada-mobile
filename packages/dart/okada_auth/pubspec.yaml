name: okada_auth
description: Okada Platform - Authentication, session management, and security
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.7
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  okada_core:
    path: ../okada_core
  okada_api:
    path: ../okada_api
  # State Management
  flutter_bloc: ^8.1.3
  # Authentication
  local_auth: ^2.1.8
  # JSON Serialization
  json_annotation: ^4.8.1
  freezed_annotation: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  bloc_test: ^9.1.5
  json_serializable: ^6.7.1
  freezed: ^2.4.6
  build_runner: ^2.4.8
  mocktail: ^1.0.1

flutter:
  uses-material-design: true
