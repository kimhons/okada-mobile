/// Test configuration and utilities

/// Delay for async operations in tests
const testAsyncDelay = Duration(milliseconds: 100);

/// Longer delay for WebSocket connection tests
const testWebSocketDelay = Duration(milliseconds: 200);

/// Test order ID
const testOrderId = 'ORD-12345';

/// Test rider ID
const testRiderId = 'RDR-678';

